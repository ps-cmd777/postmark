# Claude API key — used for synthesis (search summaries, pre-flight,
# critique, brief). See POSTMARK_PROJECT_BRIEF.md §6.
ANTHROPIC_API_KEY=

# Voyage AI key — used for embedding generation (voyage-3-large, 1024
# dims). Voyage replaces the brief's earlier "Anthropic embeddings"
# reference; Anthropic does not sell embeddings directly. See §6.
VOYAGE_API_KEY=
